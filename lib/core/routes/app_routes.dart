/// Centralized named route constants for MyWorkersApp.
///
/// Use [AppRoutes] constants when navigating with [Navigator.pushNamed] or
/// when declaring the [MaterialApp.routes] map to avoid magic strings.
class AppRoutes {
  AppRoutes._();

  static const String home = '/';
  static const String services = '/services';
  static const String serviceDetail = '/service-detail';
  static const String booking = '/booking';
}
